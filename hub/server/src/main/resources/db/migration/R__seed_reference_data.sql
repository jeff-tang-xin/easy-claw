-- ============================================================================
-- R__seed_reference_data.sql  (可重复迁移)
-- 幂等参考/字典数据。规约（§3.1）：一律 INSERT ... ON CONFLICT DO NOTHING，校验和变化即重放。
--
-- A0 说明：V1 五张都是业务表、无字典表；角色→权限映射由服务端代码按角色推导（§4.3），不落库，
-- 故 A0 阶段本文件暂无种子 DML，仅占位以固化脚本布局与可重复迁移机制。
-- 后续引入字典表（默认 plan、角色-权限字典、参考配置）时，按幂等规约在此补充。
-- ============================================================================
-- (A0: intentionally no seed DML)
